# /force
#
# Run the snake in force mode: call the model, validate, retry on failure until pass
# or retry cap. Usage: /force <task description>
#
# Example:
#   /force Write a PowerShell function that lists Azure DevOps pipelines
