# /retry
#
# Re-run the last failed snake attempt with the validation error injected as context.
# Useful when you want to manually steer a retry instead of letting the loop decide.
#
# Example:
#   /retry The output was missing the required 'basis' field
